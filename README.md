Express Router Project

Project Overview

This is a Node.js application built using Express.js that demonstrates routing using separate route files (user.js, post.js). The project follows a modular structure for better scalability and maintainability.
